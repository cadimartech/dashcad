# Despliegue en NAS (Synology, ZimaOS, etc.)
# La imagen se publica automáticamente en ghcr.io desde GitHub Actions
#
# Uso en el NAS:
#   1. docker login ghcr.io (con tu token de GitHub)
#   2. docker compose -f docker-compose.nas.yml pull
#   3. docker compose -f docker-compose.nas.yml up -d
#
services:
  dashcad:
    image: ghcr.io/cadimartech/dashcad:latest
    container_name: dashcad
    ports:
      - "3000:3000"
    volumes:
      - /ZimaOS-HD/AppData/dashcad/data/catalog:/app/catalog
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://127.0.0.1:3000/api/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
